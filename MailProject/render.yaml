databases:
  - name: mailapp-db
    databaseName: mailapp
    user: mailapp

services:
  - type: web
    name: mailapp
    runtime: python3
    buildCommand: "./build.sh"
    startCommand: "gunicorn MailProject.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mailapp-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
